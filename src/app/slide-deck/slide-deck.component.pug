.reveal
  .slides
    section(data-state="title")
      h1 Rest for the Weary
      h2 Using Spring Boot and Spring Data to create easy RESTful services
    section(*ngFor="let slideDeck of slides;")
      section(*ngFor="let slide of slideDeck")
        dynamic-html([content]="slide")
    section(data-state="title")
      h1 Thanks!
      h2 Any questions?
